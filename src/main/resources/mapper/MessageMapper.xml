<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyj.java_chatroom.model.MessageMapper">

    <select id="getLastMessage" resultType="java.lang.String">
        select content from message where sessionId=#{sessionId} order by postTime desc limit 1;
    </select>


    <select id="getHistoryMessages" resultType="com.tyj.java_chatroom.model.Message">
        select messageId,fromId,username as fromName,sessionId,content,postTime
            from message,user where message.fromId=user.userId and sessionId=#{sessionId}
        order by postTime desc limit 100
    </select>

    <insert id="add">
        insert into message values (null,#{sessionId},#{fromId},#{content},now())
    </insert>
</mapper>